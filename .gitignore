# Stale Word exports -- generated artefacts, and they predate most of the site.
# They stay on disk; they just are not part of the published repository.
*.docx

# Local editor / OS noise
.DS_Store
Thumbs.db
desktop.ini
__pycache__/
*.pyc

# Source material used for research only -- never published.
# These stay on disk; they just do not go into a public repository.
*.txt
*.pdf
